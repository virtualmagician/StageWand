/* showui_version.h — one version string for the wand UI, shown on the Setup
 * page so it's obvious which build is on which board. Bump on releases. */
#ifndef SHOWUI_VERSION_H
#define SHOWUI_VERSION_H

#define STAGEWAND_VERSION "0.2.0"

#endif /* SHOWUI_VERSION_H */
